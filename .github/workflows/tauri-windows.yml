name: Build Windows App (Tauri)

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Create .env for Vite build
        shell: bash
        run: |
          echo "VITE_SUPABASE_URL=${{ secrets.VITE_SUPABASE_URL }}" > .env
          echo "VITE_SUPABASE_ANON_KEY=${{ secrets.VITE_SUPABASE_ANON_KEY }}" >> .env

      - name: Install dependencies
        run: npm ci

      - name: Build frontend
        run: npm run build

      - name: Show tauri.conf.json used by CI
        shell: bash
        run: |
          echo "=== pwd ==="
          pwd
          echo "=== list src-tauri ==="
          ls -la src-tauri
          echo "=== tauri.conf.json ==="
          cat src-tauri/tauri.conf.json
      - name: Show tauri config used
        shell: bash
        run: |
          ls -la
          ls -la src-tauri
          echo "----- src-tauri/tauri.conf.json -----"
          cat src-tauri/tauri.conf.json
          echo "----- grep identifier/targets -----"
          grep -R --line-number '"targets"' . || true
          grep -R --line-number '"identifier"' src-tauri/tauri.conf.json || true
      - name: Build Tauri (Windows)
        shell: bash
        run: |
          npx -y @tauri-apps/cli@1.5.14 build --config src-tauri/tauri.conf.json --bundles msi,nsis

      - name: Upload Windows bundles
        uses: actions/upload-artifact@v4
        with:
          name: windows-bundles
          path: src-tauri/target/release/bundle/**
